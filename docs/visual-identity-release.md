# Visual identity publication 0.3.0

This publication advances only the concrete `nilx.one` context from `0.2.0` to `0.3.0`.

It does not change the consumed Mind Protocol release, protocol lock, canonical subject, publication owner, repository role, product identity boundary, or relationship semantics. The exact protocol remains `1.0.0-rc.2` at immutable release commit `acdcedcf02c8b4ef314179bf54955a84606c8fb5`.

The new durable context is the first canonical visual identity binding for `organization:nilx-one`: one SVG primary emblem, one explicit visual-asset catalog, SHA-256 integrity validation, and a deterministic provider projection contract. `0x1` remains a separate product identity.

<!-- © 2026 aiaiaiai · aiaiaiai.org -->
